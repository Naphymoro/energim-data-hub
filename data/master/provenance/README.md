# Provenance Layer

Records how each dataset was discovered, transformed, validated, and exported.

Main folders:

- `dataset_provenance/`
- `source_provenance/`
- `crawler_runs/`
- `transformation_logs/`

Every normalized dataset must have a dataset provenance record and transformation log before it can pass the SDMX gate.

