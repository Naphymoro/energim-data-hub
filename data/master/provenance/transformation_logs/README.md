# Transformation Logs

Each normalized dataset must have a transformation log before it can pass the SDMX gate. Logs should record source evidence IDs, extraction method, cleaning rules, unit conversions, assumptions, reviewer notes, and export outputs.

Current crawler outputs are candidate evidence only, so dataset-specific transformation logs are not yet complete.

